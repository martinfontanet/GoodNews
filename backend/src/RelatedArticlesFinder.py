# Given an image represented by a file path on the disk, this function returns links to articles that use that image
# The number parameter specifies the maximum number of responses that should be returned
# If some error happens return None
# If no results were found, return an empty list []
def findRelatedArticles(imagePath, number=10):
	return ["someurl.com", "otherurl.com"]